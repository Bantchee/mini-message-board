extends layout

block content
  h1= title
  p Welcome to #{title}
  each message in messages
    div(class="message")
      p= message.user
      p= message.text
      p= message.added
  a(href="../new")= "New Message"
